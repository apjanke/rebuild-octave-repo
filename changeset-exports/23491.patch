# HG changeset patch
# User Carnë Draug <carandraug@octave.org>
# Date 1494853778 -3600
#      Mon May 15 14:09:38 2017 +0100
# Node ID 16d77dc05af6b7a3c9b104dcdaf7b66018f06666
# Parent  7463c8e91dba4277299592d1754574d0b56d2a85
doc: fix typo 'to the linker' not 'though the linker'

diff --git a/scripts/miscellaneous/mkoctfile.m b/scripts/miscellaneous/mkoctfile.m
--- a/scripts/miscellaneous/mkoctfile.m
+++ b/scripts/miscellaneous/mkoctfile.m
@@ -49,17 +49,17 @@
 ## @item  -M
 ## @itemx --depend
 ## Generate dependency files (.d) for C and C++ source files.
 ##
 ## @item -R DIR
 ## Add the run-time path to the link command.
 ##
 ## @item @nospell{-Wl,@dots{}}
-## Pass flags though the linker like @nospell{"-Wl,-rpath=@dots{}"}.
+## Pass options to the linker like @nospell{"-Wl,-rpath=@dots{}"}.
 ## The quotes are needed since commas are interpreted as command
 ## separators.
 ##
 ## @item -W@dots{}
 ## Pass flags though the compiler like @nospell{"-Wa,OPTION"}.
 ##
 ## @item -c
 ## Compile but do not link.
